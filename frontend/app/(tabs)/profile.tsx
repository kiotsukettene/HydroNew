import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'
import  ProfileIndex  from '../account/security';

export default function Profile() {
  return <ProfileIndex />;
}